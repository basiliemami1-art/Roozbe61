*.iml
.gradle/
.idea/
build/
local.properties
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# Native cores are fetched/built by CI, never committed.
app/libs/*.aar
app/libs/*.jar

# Signing material. signing/dev-signing.jks is deliberately tracked — see
# signing/README.md; any other keystore is private and must never be committed.
*.jks
*.keystore
!signing/dev-signing.jks
keystore.properties

# Fetched by CI, never committed.
desktop/resources/windows/sing-box.exe
